services:
  - type: web
    name: m1p13mean-Brady-Antonio
    env: node
    region: frankfurt
    buildCommand: npx -p @angular/cli ng build
    startCommand: node dist/frontend/server/server.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
